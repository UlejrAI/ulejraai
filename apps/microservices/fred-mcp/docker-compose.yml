services:
  fred-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fred-mcp
    ports:
      - "8022:8080"
    environment:
      - FRED_API_KEY=${FRED_API_KEY}
      - PORT=8080
    restart: unless-stopped
